applications:
- name: hpcloudrecruit
  mem: 256M
  instances: 1
  services:
    hpcloudrecruit-db:
      type: mysql
  stackato:
    hooks:
      post-staging:
         - dbshell < setup/createdb.sql
    ignores:
    - .git
    - .venv
    - .gitignore
    - devsetup.sh
    - README.md
